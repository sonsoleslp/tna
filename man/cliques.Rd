% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/patterns.R
\name{cliques}
\alias{cliques}
\title{Identify and Visualize Cliques in a Transition Network}
\usage{
cliques(
  x,
  cluster = 1,
  n = 3,
  weight_threshold = 0,
  sum_weights = FALSE,
  minimum = 1e-05,
  mar = c(5, 5, 5, 5),
  plot = TRUE,
  show_loops = FALSE,
  ...
)
}
\arguments{
\item{x}{An object of type \code{tna}}

\item{cluster}{An integer specifying which cluster to analyze. Defaults to \code{1}.}

\item{n}{An integer specifying the size of the cliques to identify. Defaults to \code{3} (triads).}

\item{weight_threshold}{A numeric value that sets the minimum edge weight for an edge to be considered in the clique. Edges below this threshold are ignored. Defaults to \code{0}.}

\item{sum_weights}{A logical value specifying whether to sum the weights of directed edges when determining cliques. Defaults to \code{FALSE}.}

\item{minimum}{A numeric value for the minimum edge weight to be displayed in the visualization. Defaults to \code{0.00001}.}

\item{mar}{A numeric vector specifying the margins for the visualization plot. Defaults to \code{c(5,5,5,5)}.}

\item{plot}{A logical value indicating whether to visualize the identified cliques using the \code{qgraph} package. Defaults to \code{TRUE}.}

\item{show_loops}{A logical value indicating whether self-loops (edges from a node to itself) should be displayed in the visualizations. Defaults to \code{FALSE}.}

\item{...}{Additional parameters passed to the \code{qgraph} plotting function for customization.}
}
\value{
A list with two elements:
\itemize{
\item \code{count}: The total number of cliques found.
\item \code{cliques}: A list of detected cliques, where each element contains:
\itemize{
\item \code{matrix}: The submatrix representing the connections between the nodes in the clique.
\item \code{pie_values}: The initial state probabilities (\code{Pie} values) for the nodes in the clique.
}
}
}
\description{
This function identifies cliques of a specified size in a weighted transition network within a \code{tna} object. It allows for customizable visualization of the cliques and can optionally display loops in the network.
}
\details{
This function searches for cliques—complete subgraphs where every pair of nodes is connected—of size \code{n} in the transition matrix for the specified cluster in the \code{tna} object. The function loops over all combinations of nodes, checking if they form a valid clique based on the edge weights and the provided threshold.

If any cliques are found, the function can optionally visualize them using the \code{qgraph} package. The visualization can be customized with additional arguments passed through the \code{...} parameter, such as node sizes, edge thickness, and more. The function also allows controlling whether to display self-loops in the visualization with the \code{show_loops} parameter.
}
\examples{
\dontrun{
# Find and visualize 3-cliques (triads) in the first cluster
cliques_result <- cliques(my_tna_object, cluster = 1, n = 3)

# Find and visualize 4-cliques in the second cluster without showing loops
cliques_result <- cliques(my_tna_object, cluster = 2, n = 4, show_loops = FALSE)
}
}
\seealso{
\code{qgraph}
}
